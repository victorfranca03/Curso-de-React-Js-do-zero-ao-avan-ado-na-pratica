function acao() {
    document.write("</br> Executando...</br>");
}


// Executa de tempo em tempo
// setInterval(acao, 1000);

// determina um tempo para executar e executa uma vez so

// setTimeout(acao, 3000);